<div class="iw-404" style="padding-top: calc(var(--iw-ann-height) + var(--iw-nav-height)); min-height: 100vh; display: flex; align-items: center; justify-content: center; text-align: center;">
  <div>
    <img src="{{ 'dagger-white.png' | asset_url }}" alt="" style="height: 80px; opacity: 0.3; margin: 0 auto 40px;">
    <div style="font-family: var(--iw-font-mono); font-size: 11px; letter-spacing: 1.4px; color: var(--iw-gray); text-transform: uppercase; margin-bottom: 16px;">ERROR 404</div>
    <h1 style="font-family: var(--iw-font-display); font-size: clamp(48px, 8vw, 120px); font-weight: 500; text-transform: uppercase; letter-spacing: -1px; line-height: 1; margin: 0 0 32px;">NOT<br>FOUND.</h1>
    <p style="font-family: var(--iw-font-body); font-size: 16px; color: var(--iw-gray); margin: 0 0 40px;">The page you're looking for doesn't exist.</p>
    <a href="/" class="iw-btn">RETURN HOME</a>
  </div>
</div>
